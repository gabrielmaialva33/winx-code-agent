=== change cwd and fail ===

◉ <WORKSPACE>/nested──➤<PROMPT_NONCE>:1

This is the main shell. No command running in background.

---

status = process exited
exit code = 1
cwd = <WORKSPACE>/nested
=== next command ===

<WORKSPACE>/nested
◉ <WORKSPACE>/nested──➤<PROMPT_NONCE>:0

This is the main shell. No command running in background.

---

status = process exited
exit code = 0
cwd = <WORKSPACE>/nested
