# Build

Compila o projeto winx-code-agent.

## Instruções

1. Rode `cargo build --release` para build de produção
2. Rode `cargo build` para build de desenvolvimento
3. Se houver erros, analise e corrija
4. Mostre o resultado final
