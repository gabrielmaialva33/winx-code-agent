# Check

Verifica a qualidade do código Rust.

## Instruções

1. Rode `cargo clippy -- -W clippy::all` para lint
2. Rode `cargo fmt --check` para verificar formatação
3. Rode `cargo check` para verificar compilação
4. Liste todos os warnings e erros encontrados
5. Sugira correções para cada problema
